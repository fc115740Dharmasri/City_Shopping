// MongoDB support removed. This file is intentionally left as a placeholder
// to avoid runtime errors if a stale import exists. The backend is MySQL-only
// and uses Sequelize (see `config/mysql.js` and `models/index.js`).

module.exports = null;

